{
  "scripts": {
    "build": "parcel build index.html",
    "serve": "parcel index.html --port 8080 --open",
    "local": "parcel index.html --host 0.0.0.0 --port 8080"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.2"
  }
}
